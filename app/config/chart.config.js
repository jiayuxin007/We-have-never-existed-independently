/**
 * 六道数据面板 / 扇形图 — 已确认样式与配色
 */
(function (global) {
    'use strict';

    global.KARMA_CHART_CONFIG = {
        /** 已选样式：orbital（轨道节点环） */
        variant: 'orbital',
        legendLayout: 'pills',
    };
})(typeof window !== 'undefined' ? window : this);
